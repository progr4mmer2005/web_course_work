<section>
  <h1>Категории</h1>
  <form method="get" class="filters">
    <input type="text" name="search" value="{{search}}" placeholder="Поиск" />
    <button class="btn" type="submit">Найти</button>
    <a class="btn" href="/admin/categories/new">Добавить</a>
  </form>

  <table class="admin-table">
    <thead><tr><th>ID</th><th>Название</th><th>Slug</th><th>Активна</th><th>Действия</th></tr></thead>
    <tbody>
      {{#each items}}
        <tr>
          <td>{{id}}</td>
          <td>{{name}}</td>
          <td>{{slug}}</td>
          <td>{{#if is_active}}Да{{else}}Нет{{/if}}</td>
          <td>
            <a class="btn" href="/admin/categories/{{id}}/edit">Изменить</a>
            <form class="inline-form" method="post" action="/admin/categories/{{id}}/delete">
              <button class="btn" type="submit">Удалить</button>
            </form>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
</section>
